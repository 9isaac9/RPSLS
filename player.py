from ai import Ai
from human import Human

class Player:
    def __init__(self):
        self.gesture_number = ()
        self.gesture_name = ()
        self.name = "TOD"
        

    def gesture_number(self):
        self.number = ()
        if (number) == 0:
            number == Rock
        elif (number) == 1
            number = Spock
        elif (number) ==2
            number 
        number == 3
        number == 4

    def gesture_name(self):
        if (name) == ("Rock")
            name == 0